server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: acc-lab
  
  # 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/acc_system_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
  
  # 邮件配置
  mail:
    host: smtp.qq.com
    port: 465
    username: 2650090110@qq.com
    password: jcgxyafodrtvecdg
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            required: true
          starttls:
            enable: false
            required: false
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    default-encoding: UTF-8
  
  # 静态资源配置
  web:
    resources:
      static-locations: classpath:/static/,file:./,file:./photos/
      add-mappings: true

# JWT 配置
jwt:
  secret: your-super-secret-key-that-no-one-knows
  expiration: 3600000 # 1小时，单位：毫秒

